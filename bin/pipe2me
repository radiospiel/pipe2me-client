#!/usr/bin/env ruby
$:.unshift "#{File.dirname(__FILE__)}/../lib"

require "pipe2me"
require "pipe2me/cli"

begin
  Pipe2me::CLI.start ARGV
rescue
  if UI.verbosity >= 2
    UI.error $!
    raise
  elsif UI.verbosity >= 0
    UI.error $!
  else
    STDERR.puts $!
  end
  exit 1
end
